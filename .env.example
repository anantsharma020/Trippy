# Trippy environment

# Leave these unset to run in LOCAL mode (data stays in the browser).
# Fill them in to enable cloud accounts, cross-device login, and friends.
# Find them in your Supabase project: Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
